{
  "name": "ferhat-patisserie",
  "private": true,
  "version": "1.0.0",
  "description": "Ferhat Patisserie — static site + request form Worker (Cloudflare)",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
